// discord webhooks
/*
import "dotenv/config";
import { WebhookClient } from "discord.js";

const webhook: WebhookClient = new WebhookClient
              (process.env.webhook.id, 
               process.env.webhook.token);

webhook.send("started up");
process.on("exit", () => webhook.send("closed"));
*/
